<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>My Reserves</title>
    <link rel="stylesheet" href="/output.css" />
    <link rel="icon" href="/icon.png">
  </head>
  <body class="bg-gradient-to-br from-red-100 to-orange-100">
    <div class="flex flex-col min-h-screen">
      <%- include('../partials/navbar.ejs') %>

      <!-- Centered Card Container -->
      <div class="flex flex-1 items-center justify-center px-6 py-12">
        <div
          class="w-full max-w-md bg-gradient-to-br from-red-300 via-rose-400 to-red-600 rounded-xl shadow-xl p-8 text-center text-white"
        >
          <h2 class="mb-4 text-xl font-semibold">
            This (Reserve) page will be added soon.
          </h2>
          <p class="font-medium">
            <button
              class="mt-2 px-4 py-2 bg-white bg-opacity-20 hover:bg-opacity-30 transition rounded-md backdrop-blur-sm text-black font-semibold"
            >
              We're working on it! Check back later.
            </button>
          </p>
        </div>
      </div>

      <%- include('../partials/footer.ejs') %>
    </div>
  </body>
</html>
