<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= home.location %> | StayOrbit</title>
    <link rel="stylesheet" href="/output.css" />
    <link rel="icon" href="/icon.png" />
  </head>
  <body
    class="bg-gradient-to-tr from-rose-100 via-orange-100 to-yellow-100 text-gray-800 font-sans"
  >
    <%- include('../partials/loader.ejs') %>
    <div class="flex flex-col min-h-screen">
      <%- include('../partials/navbar.ejs') %>

      <!-- Hero Banner -->
      <section
        class="relative w-full h-[65vh] overflow-hidden shadow-lg rounded-b-2xl"
      >
        <img
          src="<%= home.image %>"
          alt="Stay Image"
          class="object-cover w-full h-full filter brightness-75"
        />
        <div
          class="absolute inset-0 bg-gradient-to-r from-black/40 to-black/10"
        ></div>

        <!-- Hero Text -->
        <div
          class="absolute inset-0 flex flex-col justify-center items-start px-8 sm:px-20 text-white z-10"
        >
          <h1 class="text-4xl sm:text-5xl font-extrabold drop-shadow-lg">
            Escape to <span class="text-yellow-300"><%= home.location %></span>
          </h1>
          <p class="mt-2 text-lg sm:text-xl font-medium">
            Welcome to
            <span
              class="font-extrabold bg-gradient-to-r from-yellow-400 to-pink-400 text-transparent bg-clip-text drop-shadow-md"
            >
              <%= home.homename %>
            </span>
            — your cozy getaway!
          </p>
          <a
            href="/"
            class="mt-6 px-5 py-2.5 text-sm font-semibold bg-gradient-to-r from-green-400 to-emerald-500 text-white rounded-lg shadow-lg hover:brightness-110 transition duration-300"
          >
            ← Back to Home
          </a>
        </div>
      </section>

      <!-- Main Details Section -->
      <section class="relative z-10 -mt-20 px-4 sm:px-10">
        <div
          class="max-w-5xl mx-auto bg-white/40 backdrop-blur-lg border border-white/30 rounded-2xl shadow-xl p-6 sm:p-10"
        >
          <div class="grid grid-cols-1 md:grid-cols-2 gap-10">
            <!-- Details -->
            <div class="space-y-5">
              <h2 class="text-xl font-semibold text-orange-800 mb-3">
                🏡 Stay Highlights
              </h2>

              <p class="text-base">
                <span class="font-semibold text-gray-900">📍 Location:</span>
                <%= home.location %>
              </p>
              <p class="text-base">
                <span class="font-semibold text-gray-900">💵 Price:</span> ₹<%=
                home.price %>
                <span class="text-sm text-gray-700">per night</span>
              </p>
              <p class="text-base">
                <span class="font-semibold text-gray-900">☎️ Contact:</span>
                <%= home.contactdetails %>
              </p>
              <p class="text-base">
                <span class="font-semibold text-gray-900"
                  >❄️ AC Availability:</span
                >
                <%= home.ac_service %>
              </p>

              <!-- Description -->
              <div class="mt-6">
                <h3 class="text-xl font-semibold text-orange-800 mb-2">
                  📖 About this Stay
                </h3>
                <p class="text-sm sm:text-base leading-relaxed text-gray-800">
                  <%= home.description && home.description.trim().length > 0 ?
                  home.description : "No description provided for this stay." %>
                </p>
              </div>
            </div>

            <!-- Image Preview -->
            <div
              class="w-full h-64 sm:h-72 md:h-full overflow-hidden rounded-xl border border-white/30 shadow-lg"
            >
              <img
                src="<%= home.image %>"
                alt="Stay Preview"
                class="w-full h-full object-cover"
              />
            </div>
          </div>

          <!-- Actions -->
          <div
            class="mt-10 w-full flex flex-col sm:flex-row flex-wrap justify-center gap-4"
          >
            <!-- Explore More -->
            <a
              href="/all-homelist"
              class="px-6 py-3 text-white text-sm font-semibold text-center rounded-lg bg-gradient-to-r from-blue-600 to-indigo-700 shadow-md hover:scale-105 transition-transform flex items-center justify-center"
            >
              🔙 Explore More Stays
            </a>

            <!-- Add to Favorites -->
            <form action="/favorites/<%= home._id %>" method="POST">
              <button
                type="submit"
                class="px-6 py-3 text-white text-sm font-semibold text-center rounded-lg bg-gradient-to-r from-pink-500 to-red-600 shadow-md hover:scale-105 transition-transform flex items-center justify-center w-full hover:cursor-pointer"
              >
                ❤️ Add to Favorites
              </button>
            </form>
          </div>
        </div>
      </section>

      <%- include('../partials/footer.ejs') %>
    </div>
    <script src="/js/loaderLogic.js"></script>
  </body>
</html>
